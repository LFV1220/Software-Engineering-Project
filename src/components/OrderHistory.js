import React from 'react';

function OrderHistory({ orders, setOrders }) {

    return (
        <section>
            <p>Test (from orderHistory component)</p>
        </section>
    )
}

export default OrderHistory;